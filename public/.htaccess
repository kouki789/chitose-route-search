Options -Indexes
RewriteEngine On
RewriteBase /

# 実ファイル・ディレクトリはそのまま通す
RewriteCond %{REQUEST_FILENAME} -f [OR]
RewriteCond %{REQUEST_FILENAME} -d
RewriteRule ^ - [L]

# React Router の SPA フォールバック
RewriteRule ^ index.html [L]
